# Dependências enxutas para o portão de CI (lint + types + pytest hermético).
# NÃO inclui torch / sentence-transformers / scikit-surprise — os smoke tests
# rodam sem modelo. O eval completo (eval-gate.yml) usa requirements.txt.
ruff==0.16.5
mypy==1.19.1
pytest>=8
flask==3.0.3
Flask-Limiter>=3.5
Flask-Login>=0.6
Flask-WTF>=1.2
argon2-cffi>=23
email-validator>=2
prometheus-client>=0.20
pandera>=0.20
pydantic>=2
fastapi>=0.110
httpx>=0.27
numpy>=1.22,<2.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0
rapidfuzz>=3.0
SQLAlchemy==2.0.29
python-dotenv==1.0.1
requests==2.31.0
