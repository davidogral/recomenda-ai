# Todas as dependências do projeto:
flask==3.0.3
pandas>=2.0.0,<3.0.0
numpy>=1.22.0,<2.0.0
scikit-learn==1.4.0
scipy>=1.10.0,<2.0.0
scikit-surprise>=1.1.3
sentence-transformers>=2.2.2
rapidfuzz>=3.0.0
nltk>=3.8
psutil>=5.9          # medição de RSS no perfil de latência (eval/latency.py)
pandera>=0.20        # validação de schema do catálogo / dump IMDb (core/schemas.py)
SQLAlchemy==2.0.29
# --- opcional: encoder ONNX INT8 (retrieval/onnx_embed.py, eval/bench.py) ---
onnx>=1.16
onnxruntime>=1.17
python-dotenv==1.0.1
requests==2.31.0
gunicorn==23.0.0
# --- observabilidade + limites (produção) ---
prometheus-client>=0.20
Flask-Limiter>=3.5
httpx>=0.27          # cliente do serviço de inferência (core/inference_client.py)
